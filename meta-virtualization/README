The meta-virtualization layer has moved from being part of wr-kernel to a
standalone layer.  You must update your build's conf/bblayers.conf to point
to layers/meta-virtualization.

This can be done with: \
sed -i 's|wr-kernel/\(meta-virtualization\)|\1|' conf/bblayers.conf \
