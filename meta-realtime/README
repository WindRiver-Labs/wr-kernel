The meta-realtime layer has moved from being part of wr-kernel to a
standalone layer.  You must update your build's conf/bblayers.conf to point
to layers/meta-realtime.

This can be done with: \
sed -i 's|wr-kernel/\(meta-realtime\)|\1|' conf/bblayers.conf \
